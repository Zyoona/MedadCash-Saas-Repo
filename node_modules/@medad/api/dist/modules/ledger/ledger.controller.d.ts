import { LedgerService } from './ledger.service.js';
import { type AuthUser } from '../auth/auth.guard.js';
export declare class LedgerController {
    private readonly ledger;
    constructor(ledger: LedgerService);
    post(user: AuthUser, body: {
        branchId?: string;
        fiscalYearId: string;
        date: string;
        sourceType: string;
        sourceId: string;
        memo?: string;
        lines: {
            accountCode: string;
            debitAgora: number;
            creditAgora: number;
            customerId?: string;
            supplierId?: string;
        }[];
    }): Promise<string>;
    listEntries(user: AuthUser, branchId?: string, from?: string, to?: string, sourceType?: string, page?: string, pageSize?: string): Promise<{
        total: number;
        page: number;
        pageSize: number;
        rows: unknown[];
    }>;
    getEntry(user: AuthUser, id: string): Promise<{}>;
    /** The ONLY correction path: a NEW mirrored entry (§1.2 — reversal only). */
    reverse(user: AuthUser, id: string, body: {
        reason: string;
        branchId?: string;
        fiscalYearId: string;
        date?: string;
    }): Promise<string>;
    trialBalance(user: AuthUser, branchId?: string, byAccount?: string): Promise<{
        rows: {
            debitAgora: number;
            creditAgora: number;
            balanceAgora: number;
            id: string;
            code: string;
            name: string;
            type: string;
            isClosed: boolean;
        }[];
        totalDrAgora: number;
        totalCrAgora: number;
        balanced: boolean;
    }> | Promise<{
        debitAgora: number;
        creditAgora: number;
        balanced: boolean;
    }>;
    /** Legacy unauthenticated path kept for local smoke tests. */
    trialBalanceLegacy(tenantId: string, branchId?: string): Promise<{
        debitAgora: number;
        creditAgora: number;
        balanced: boolean;
    }>;
}
//# sourceMappingURL=ledger.controller.d.ts.map
"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.PartiesService = void 0;
const common_1 = require("@nestjs/common");
const prisma_service_js_1 = require("../../prisma/prisma.service.js");
const ledger_service_js_1 = require("../ledger/ledger.service.js");
const money_util_js_1 = require("../../common/money.util.js");
const ctx_js_1 = require("../../common/ctx.js");
const AR = '1300';
const AP = '2000';
const OPENING = '3900';
let PartiesService = class PartiesService {
    prisma;
    ledger;
    constructor(prisma, ledger) {
        this.prisma = prisma;
        this.ledger = ledger;
    }
    // ─── Customers (فرعي + افتتاحي + حد دين §3) ───
    async listCustomers(tenantId, branchId) {
        const rows = await this.prisma.customer.findMany({
            where: { tenantId, deletedAt: null, ...(branchId ? { branchId } : {}) },
            orderBy: { name: 'asc' },
        });
        const balances = await this.balancesByParty(tenantId, AR, 'customerId');
        return rows.map((c) => ({
            ...c,
            openingBalanceAgora: (0, money_util_js_1.decToAgora)(c.openingBalance),
            creditLimitAgora: (0, money_util_js_1.decToAgora)(c.creditLimit),
            balanceAgora: balances.get(c.id) ?? 0,
        }));
    }
    /** Dr-Cr sums of journal lines tagged with customerId/supplierId on AR/AP account. */
    async balancesByParty(tenantId, accountCode, partyField) {
        const lines = await this.prisma.journalLine.findMany({
            where: { entry: { tenantId }, account: { code: accountCode }, [partyField]: { not: null } },
            select: { debit: true, credit: true, customerId: true, supplierId: true },
        });
        const map = new Map();
        for (const l of lines) {
            const key = (partyField === 'customerId' ? l.customerId : l.supplierId);
            map.set(key, (map.get(key) ?? 0) + (0, money_util_js_1.decToAgora)(l.debit) - (0, money_util_js_1.decToAgora)(l.credit));
        }
        return map;
    }
    async customerBalance(tenantId, customerId) {
        const m = await this.balancesByParty(tenantId, AR, 'customerId');
        return m.get(customerId) ?? 0;
    }
    async supplierBalance(tenantId, supplierId) {
        const m = await this.balancesByParty(tenantId, AP, 'supplierId');
        return -(m.get(supplierId) ?? 0); // AP is credit-normal: return positive owed amount
    }
    async createCustomer(tenantId, input, actorId) {
        if (!input.name?.trim())
            throw new common_1.BadRequestException('اسم العميل مطلوب');
        const opening = input.openingBalanceAgora ?? 0;
        if (!Number.isInteger(opening))
            throw new common_1.BadRequestException('الرصيد الافتتاحي أغورات صحيحة');
        const date = input.openingDate ? new Date(input.openingDate) : new Date();
        const fy = await (0, ctx_js_1.fiscalYearFor)(this.prisma, tenantId, date);
        return this.prisma.$transaction(async (db) => {
            const c = await db.customer.create({
                data: {
                    tenantId, branchId: input.branchId, name: input.name.trim(), phone: input.phone ?? null,
                    openingBalance: (0, money_util_js_1.agoraToDec)(opening), openingDate: opening !== 0 ? date : null,
                    creditLimit: (0, money_util_js_1.agoraToDec)(input.creditLimitAgora ?? 0),
                    paymentTerms: input.paymentTerms ?? null,
                },
            });
            if (opening !== 0) {
                // Opening debit balance (مدين): Dr AR / Cr 3900. Negative opening → mirrored.
                const lines = opening > 0
                    ? [
                        { accountCode: AR, debitAgora: opening, creditAgora: 0, customerId: c.id },
                        { accountCode: OPENING, debitAgora: 0, creditAgora: opening },
                    ]
                    : [
                        { accountCode: OPENING, debitAgora: -opening, creditAgora: 0 },
                        { accountCode: AR, debitAgora: 0, creditAgora: -opening, customerId: c.id },
                    ];
                await this.ledger.post({
                    tenantId, branchId: input.branchId, fiscalYearId: fy.id, date,
                    sourceType: 'opening_balance', sourceId: c.id,
                    memo: `رصيد افتتاحي لعميل: ${c.name}`, lines,
                }, db);
            }
            await (0, ctx_js_1.auditTx)(db, { tenantId, actorId, branchId: input.branchId, action: 'create', entity: 'customers', entityId: c.id, diff: input });
            await (0, ctx_js_1.syncOpTx)(db, { tenantId, branchId: input.branchId, entity: 'customer', entityId: c.id, op: 'create', payload: { name: c.name, phone: c.phone, openingBalanceAgora: opening, creditLimitAgora: input.creditLimitAgora ?? 0 } });
            return c;
        });
    }
    async updateCustomer(tenantId, id, patch, actorId) {
        const c = await this.prisma.customer.findFirst({ where: { id, tenantId, deletedAt: null } });
        if (!c)
            throw new common_1.NotFoundException('العميل غير موجود');
        return this.prisma.$transaction(async (db) => {
            const updated = await db.customer.update({
                where: { id },
                data: {
                    ...(patch.name !== undefined ? { name: patch.name } : {}),
                    ...(patch.phone !== undefined ? { phone: patch.phone } : {}),
                    ...(patch.creditLimitAgora !== undefined ? { creditLimit: (0, money_util_js_1.agoraToDec)(patch.creditLimitAgora) } : {}),
                    ...(patch.paymentTerms !== undefined ? { paymentTerms: patch.paymentTerms } : {}),
                },
            });
            await (0, ctx_js_1.auditTx)(db, { tenantId, actorId, branchId: c.branchId, action: 'update', entity: 'customers', entityId: id, diff: patch });
            await (0, ctx_js_1.syncOpTx)(db, { tenantId, branchId: c.branchId, entity: 'customer', entityId: id, op: 'update', payload: { ...patch, baseUpdatedAt: c.createdAt } });
            return updated;
        });
    }
    /** تحصيل من عميل (§4): Dr صندوق/بنك Cr ذمم العميل. */
    async collectFromCustomer(tenantId, branchId, input, actorId) {
        if (!Number.isInteger(input.amountAgora) || input.amountAgora <= 0)
            throw new common_1.BadRequestException('مبلغ غير صالح');
        const c = await this.prisma.customer.findFirst({ where: { id: input.customerId, tenantId, deletedAt: null } });
        if (!c)
            throw new common_1.NotFoundException('العميل غير موجود');
        const date = input.date ? new Date(input.date) : new Date();
        const fy = await (0, ctx_js_1.fiscalYearFor)(this.prisma, tenantId, date);
        return this.prisma.$transaction(async (db) => {
            const entryId = await this.ledger.post({
                tenantId, branchId, fiscalYearId: fy.id, date,
                sourceType: 'collection', sourceId: c.id,
                memo: input.memo ?? `تحصيل من ${c.name}`,
                lines: [
                    { accountCode: input.accountCode, debitAgora: input.amountAgora, creditAgora: 0 },
                    { accountCode: AR, debitAgora: 0, creditAgora: input.amountAgora, customerId: c.id },
                ],
            }, db);
            await (0, ctx_js_1.auditTx)(db, { tenantId, actorId, branchId, action: 'collection', entity: 'customers', entityId: c.id, diff: { ...input, entryId } });
            await (0, ctx_js_1.syncOpTx)(db, { tenantId, branchId, entity: 'collection', entityId: entryId, op: 'create', payload: { customerId: c.id, amountAgora: input.amountAgora, accountCode: input.accountCode } });
            return { entryId };
        });
    }
    // ─── Suppliers (tenant-level §3) ───
    async listSuppliers(tenantId) {
        const rows = await this.prisma.supplier.findMany({ where: { tenantId, deletedAt: null }, orderBy: { name: 'asc' } });
        const balances = await this.balancesByParty(tenantId, AP, 'supplierId');
        return rows.map((s) => ({
            ...s,
            openingBalanceAgora: (0, money_util_js_1.decToAgora)(s.openingBalance),
            balanceAgora: -(balances.get(s.id) ?? 0),
        }));
    }
    async createSupplier(tenantId, branchId, input, actorId) {
        if (!input.name?.trim())
            throw new common_1.BadRequestException('اسم المورد مطلوب');
        const opening = input.openingBalanceAgora ?? 0;
        const date = input.openingDate ? new Date(input.openingDate) : new Date();
        const fy = opening !== 0 ? await (0, ctx_js_1.fiscalYearFor)(this.prisma, tenantId, date) : null;
        return this.prisma.$transaction(async (db) => {
            const s = await db.supplier.create({
                data: {
                    tenantId, name: input.name.trim(), phone: input.phone ?? null,
                    openingBalance: (0, money_util_js_1.agoraToDec)(opening), openingDate: opening !== 0 ? date : null,
                },
            });
            if (opening !== 0 && fy) {
                // Supplier opening credit (دائن): Dr 3900 / Cr AP.
                const lines = opening > 0
                    ? [
                        { accountCode: OPENING, debitAgora: opening, creditAgora: 0 },
                        { accountCode: AP, debitAgora: 0, creditAgora: opening, supplierId: s.id },
                    ]
                    : [
                        { accountCode: AP, debitAgora: -opening, creditAgora: 0, supplierId: s.id },
                        { accountCode: OPENING, debitAgora: 0, creditAgora: -opening },
                    ];
                await this.ledger.post({
                    tenantId, branchId: branchId ?? '', fiscalYearId: fy.id, date,
                    sourceType: 'opening_balance', sourceId: s.id,
                    memo: `رصيد افتتاحي لمورد: ${s.name}`, lines,
                }, db);
            }
            await (0, ctx_js_1.auditTx)(db, { tenantId, actorId, branchId, action: 'create', entity: 'suppliers', entityId: s.id, diff: input });
            await (0, ctx_js_1.syncOpTx)(db, { tenantId, branchId: branchId ?? '', entity: 'supplier', entityId: s.id, op: 'create', payload: { name: s.name, phone: s.phone, openingBalanceAgora: opening } });
            return s;
        });
    }
    async updateSupplier(tenantId, id, patch, actorId) {
        const s = await this.prisma.supplier.findFirst({ where: { id, tenantId, deletedAt: null } });
        if (!s)
            throw new common_1.NotFoundException('المورد غير موجود');
        return this.prisma.$transaction(async (db) => {
            const updated = await db.supplier.update({
                where: { id },
                data: { ...(patch.name !== undefined ? { name: patch.name } : {}), ...(patch.phone !== undefined ? { phone: patch.phone } : {}) },
            });
            await (0, ctx_js_1.auditTx)(db, { tenantId, actorId, action: 'update', entity: 'suppliers', entityId: id, diff: patch });
            return updated;
        });
    }
    /** دفع لمورد (§4): Dr ذمم المورد Cr صندوق/بنك. */
    async paySupplier(tenantId, branchId, input, actorId) {
        if (!Number.isInteger(input.amountAgora) || input.amountAgora <= 0)
            throw new common_1.BadRequestException('مبلغ غير صالح');
        const s = await this.prisma.supplier.findFirst({ where: { id: input.supplierId, tenantId, deletedAt: null } });
        if (!s)
            throw new common_1.NotFoundException('المورد غير موجود');
        const date = input.date ? new Date(input.date) : new Date();
        const fy = await (0, ctx_js_1.fiscalYearFor)(this.prisma, tenantId, date);
        return this.prisma.$transaction(async (db) => {
            const entryId = await this.ledger.post({
                tenantId, branchId, fiscalYearId: fy.id, date,
                sourceType: 'supplier_payment', sourceId: s.id,
                memo: input.memo ?? `دفع للمورد ${s.name}`,
                lines: [
                    { accountCode: AP, debitAgora: input.amountAgora, creditAgora: 0, supplierId: s.id },
                    { accountCode: input.accountCode, debitAgora: 0, creditAgora: input.amountAgora },
                ],
            }, db);
            await (0, ctx_js_1.auditTx)(db, { tenantId, actorId, branchId, action: 'supplier_payment', entity: 'suppliers', entityId: s.id, diff: { ...input, entryId } });
            await (0, ctx_js_1.syncOpTx)(db, { tenantId, branchId, entity: 'supplier_payment', entityId: entryId, op: 'create', payload: { supplierId: s.id, amountAgora: input.amountAgora, accountCode: input.accountCode } });
            return { entryId };
        });
    }
    /** Default cash customer per branch (زبون نقدي افتراضي §3). */
    async cashCustomer(db, tenantId, branchId) {
        const existing = await db.customer.findFirst({ where: { tenantId, branchId, isCashDefault: true, deletedAt: null }, select: { id: true } });
        if (existing)
            return existing;
        return db.customer.create({ data: { tenantId, branchId, name: 'زبون نقدي', isCashDefault: true } });
    }
    async ensureCashCustomer(tenantId, branchId) {
        const dup = await this.prisma.customer.findFirst({ where: { tenantId, branchId, isCashDefault: true } });
        if (dup)
            throw new common_1.ConflictException('يوجد زبون نقدي مسبقاً');
        return this.prisma.customer.create({ data: { tenantId, branchId, name: 'زبون نقدي', isCashDefault: true } });
    }
};
exports.PartiesService = PartiesService;
exports.PartiesService = PartiesService = __decorate([
    (0, common_1.Injectable)(),
    __metadata("design:paramtypes", [prisma_service_js_1.PrismaService, ledger_service_js_1.LedgerService])
], PartiesService);
//# sourceMappingURL=parties.service.js.map
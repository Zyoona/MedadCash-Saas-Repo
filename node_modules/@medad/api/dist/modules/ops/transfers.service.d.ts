import { PrismaService } from '../../prisma/prisma.service.js';
import { LedgerService } from '../ledger/ledger.service.js';
import { InventoryService } from '../inventory/inventory.service.js';
export declare class TransfersService {
    private readonly prisma;
    private readonly ledger;
    private readonly inventory;
    constructor(prisma: PrismaService, ledger: LedgerService, inventory: InventoryService);
    create(tenantId: string, input: {
        fromBranchId: string;
        toBranchId: string;
        lines: {
            variantId: string;
            qty: number;
        }[];
    }, actorId: string): Promise<{
        transfer: any;
        totalCostAgora: number;
        entryId: string | null;
        warnings: string[];
    }>;
    receive(tenantId: string, id: string, actorId: string): Promise<{
        transfer: any;
        totalCostAgora: number;
        entryId: string | null;
    }>;
    list(tenantId: string, status?: string): import(".prisma/client").Prisma.PrismaPromise<({
        lines: {
            id: string;
            variantId: string;
            qty: number;
            costAgora: number;
            transferId: string;
        }[];
        fromBranch: {
            name: string;
        };
        toBranch: {
            name: string;
        };
    } & {
        tenantId: string;
        id: string;
        createdAt: Date;
        status: string;
        fromBranchId: string;
        toBranchId: string;
    })[]>;
}
//# sourceMappingURL=transfers.service.d.ts.map
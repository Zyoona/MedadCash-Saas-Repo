import { PrismaService } from '../../prisma/prisma.service.js';
import { LedgerService } from '../ledger/ledger.service.js';
export declare class FiscalService {
    private readonly prisma;
    private readonly ledger;
    constructor(prisma: PrismaService, ledger: LedgerService);
    list(tenantId: string): import(".prisma/client").Prisma.PrismaPromise<{
        tenantId: string;
        name: string;
        id: string;
        createdAt: Date;
        isClosed: boolean;
        startDate: Date;
        endDate: Date;
    }[]>;
    create(tenantId: string, input: {
        name: string;
        startDate: string;
        endDate: string;
    }, actorId: string): Promise<any>;
    /**
     * Close a fiscal year with closing entries (§4): move all revenue/expense balances
     * of the year into retained earnings (3500), then mark the year closed.
     * The ledger rejects any posting into a closed year afterwards.
     */
    close(tenantId: string, fyId: string, branchId: string, actorId: string): Promise<any>;
}
//# sourceMappingURL=fiscal.service.d.ts.map
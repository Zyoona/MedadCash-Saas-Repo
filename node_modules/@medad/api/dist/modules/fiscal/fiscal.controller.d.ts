import { FiscalService } from './fiscal.service.js';
import { type AuthUser } from '../auth/auth.guard.js';
export declare class FiscalController {
    private readonly fiscal;
    constructor(fiscal: FiscalService);
    list(user: AuthUser): import(".prisma/client").Prisma.PrismaPromise<{
        tenantId: string;
        name: string;
        id: string;
        createdAt: Date;
        isClosed: boolean;
        startDate: Date;
        endDate: Date;
    }[]>;
    create(user: AuthUser, body: {
        name: string;
        startDate: string;
        endDate: string;
    }): Promise<any>;
    close(user: AuthUser, id: string, body: {
        branchId?: string;
    }): Promise<any>;
}
//# sourceMappingURL=fiscal.controller.d.ts.map
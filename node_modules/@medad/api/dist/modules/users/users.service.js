"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.UsersService = void 0;
const common_1 = require("@nestjs/common");
const bcrypt = __importStar(require("bcryptjs"));
const prisma_service_js_1 = require("../../prisma/prisma.service.js");
const permissions_js_1 = require("../../common/permissions.js");
const ctx_js_1 = require("../../common/ctx.js");
let UsersService = class UsersService {
    prisma;
    constructor(prisma) {
        this.prisma = prisma;
    }
    list(tenantId) {
        return this.prisma.user.findMany({
            where: { tenantId, deletedAt: null },
            select: { id: true, email: true, name: true, role: true, branchId: true, isActive: true, permissionsOverride: true, createdAt: true },
            orderBy: { createdAt: 'asc' },
        });
    }
    async create(tenantId, input, actorId) {
        if (!permissions_js_1.ROLES.includes(input.role))
            throw new common_1.BadRequestException(`دور غير معروف: ${input.role}`);
        const email = input.email.toLowerCase().trim();
        const dup = await this.prisma.user.findFirst({ where: { tenantId, email } });
        if (dup)
            throw new common_1.ConflictException('البريد مستخدم مسبقاً');
        const hash = await bcrypt.hash(input.password, 12);
        const u = await this.prisma.$transaction(async (db) => {
            const created = await db.user.create({
                data: {
                    tenantId, email, name: input.name, role: input.role,
                    branchId: input.branchId ?? null,
                    passwordHash: hash,
                    permissionsOverride: input.permissionsOverride ?? undefined,
                },
                select: { id: true, email: true, name: true, role: true, branchId: true, isActive: true, permissionsOverride: true },
            });
            await (0, ctx_js_1.auditTx)(db, { tenantId, actorId, action: 'create', entity: 'users', entityId: created.id, diff: { email, role: input.role } });
            return created;
        });
        return u;
    }
    async update(tenantId, id, patch, actorId) {
        const u = await this.prisma.user.findFirst({ where: { id, tenantId, deletedAt: null } });
        if (!u)
            throw new common_1.NotFoundException('المستخدم غير موجود');
        if (patch.role && !permissions_js_1.ROLES.includes(patch.role))
            throw new common_1.BadRequestException(`دور غير معروف`);
        const updated = await this.prisma.$transaction(async (db) => {
            const row = await db.user.update({
                where: { id },
                data: {
                    ...(patch.name !== undefined ? { name: patch.name } : {}),
                    ...(patch.role !== undefined ? { role: patch.role } : {}),
                    ...(patch.branchId !== undefined ? { branchId: patch.branchId } : {}),
                    ...(patch.isActive !== undefined ? { isActive: patch.isActive } : {}),
                    ...(patch.permissionsOverride !== undefined ? { permissionsOverride: patch.permissionsOverride } : {}),
                },
                select: { id: true, email: true, name: true, role: true, branchId: true, isActive: true, permissionsOverride: true },
            });
            await (0, ctx_js_1.auditTx)(db, { tenantId, actorId, action: 'update', entity: 'users', entityId: id, diff: patch });
            return row;
        });
        return updated;
    }
    async resetPassword(tenantId, id, newPassword, actorId) {
        const u = await this.prisma.user.findFirst({ where: { id, tenantId, deletedAt: null } });
        if (!u)
            throw new common_1.NotFoundException('المستخدم غير موجود');
        const hash = await bcrypt.hash(newPassword, 12);
        await this.prisma.$transaction(async (db) => {
            await db.user.update({ where: { id }, data: { passwordHash: hash } });
            await (0, ctx_js_1.auditTx)(db, { tenantId, actorId, action: 'reset_password', entity: 'users', entityId: id });
        });
        return { ok: true };
    }
    /** Effective permission matrix for UI display. */
    matrix(tenantId) {
        return this.prisma.user.findMany({
            where: { tenantId, deletedAt: null },
            select: { id: true, name: true, role: true, permissionsOverride: true },
        }).then((rows) => rows.map((r) => ({
            id: r.id, name: r.name, role: r.role,
            perms: (0, permissions_js_1.expandPerms)(r.role, (r.permissionsOverride ?? null)),
        })));
    }
    static allPerms() {
        return permissions_js_1.PERMS;
    }
};
exports.UsersService = UsersService;
exports.UsersService = UsersService = __decorate([
    (0, common_1.Injectable)(),
    __metadata("design:paramtypes", [prisma_service_js_1.PrismaService])
], UsersService);
//# sourceMappingURL=users.service.js.map
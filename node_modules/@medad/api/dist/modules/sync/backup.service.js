"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.BackupService = exports.DriveBackupProvider = exports.LocalBackupProvider = void 0;
const common_1 = require("@nestjs/common");
const crypto = __importStar(require("node:crypto"));
const fs = __importStar(require("node:fs"));
const path = __importStar(require("node:path"));
const prisma_service_js_1 = require("../../prisma/prisma.service.js");
const env_js_1 = require("../../common/env.js");
const crypto_util_js_1 = require("../../common/crypto.util.js");
const ctx_js_1 = require("../../common/ctx.js");
const DUMP_ORDER = [
    'tenant', 'branch', 'user', 'account', 'fiscalYear', 'setting',
    'category', 'brand', 'unit', 'customer', 'supplier',
    'product', 'productVariant', 'productUnit', 'costComponent', 'productBranch', 'branchStock',
    'shift', 'purchase', 'purchaseLine', 'supplierPayment',
    'invoice', 'invoiceLine', 'invoicePayment',
    'saleReturn', 'saleReturnLine', 'quotation', 'quotationLine',
    'stockTransfer', 'stockTransferLine', 'stockCount', 'stockCountLine',
    'task', 'check', 'journalEntry', 'journalLine',
    'syncOperation', 'syncConflict', 'auditLog', 'backupRun',
];
// Child tables have no tenantId column — filter through the parent relation.
const PARENT_REL = {
    journalLine: 'entry', purchaseLine: 'purchase', supplierPayment: 'purchase',
    invoiceLine: 'invoice', invoicePayment: 'invoice', saleReturnLine: 'ret',
    quotationLine: 'quotation', stockTransferLine: 'transfer', stockCountLine: 'count',
};
function dumpWhere(model, tenantId) {
    if (model === 'tenant')
        return { id: tenantId };
    if (PARENT_REL[model])
        return { [PARENT_REL[model]]: { tenantId } };
    return { tenantId };
}
let LocalBackupProvider = class LocalBackupProvider {
    prisma;
    name = 'local';
    constructor(prisma) {
        this.prisma = prisma;
    }
    async backup(tenantId) {
        fs.mkdirSync(env_js_1.BACKUP_DIR, { recursive: true });
        const dump = { exportedAt: [new Date().toISOString()], tenantId: [tenantId] };
        for (const model of DUMP_ORDER) {
            const delegate = this.prisma[model];
            dump[model] = await delegate.findMany({ where: dumpWhere(model, tenantId) });
        }
        const ts = new Date().toISOString().replace(/[:.]/g, '-');
        const filePath = path.join(env_js_1.BACKUP_DIR, `medad-${tenantId.slice(0, 8)}-${ts}.json`);
        fs.writeFileSync(filePath, JSON.stringify(dump), 'utf8');
        return { filePath, bytes: fs.statSync(filePath).size };
    }
};
exports.LocalBackupProvider = LocalBackupProvider;
exports.LocalBackupProvider = LocalBackupProvider = __decorate([
    (0, common_1.Injectable)(),
    __metadata("design:paramtypes", [prisma_service_js_1.PrismaService])
], LocalBackupProvider);
let DriveBackupProvider = class DriveBackupProvider {
    prisma;
    local;
    name = 'drive';
    constructor(prisma, local) {
        this.prisma = prisma;
        this.local = local;
    }
    async loadCfg(tenantId) {
        const cfgRaw = await this.prisma.setting.findFirst({ where: { tenantId, branchId: null, key: 'drive_config' } });
        if (!cfgRaw)
            return { configured: false };
        return typeof cfgRaw.value === 'string' && cfgRaw.value.startsWith('v1:')
            ? (0, crypto_util_js_1.decryptJson)(cfgRaw.value)
            : cfgRaw.value;
    }
    /** Exchange the long-lived refresh token for a short-lived access token (before each upload). */
    async refreshAccessToken(cfg) {
        const res = await fetch('https://oauth2.googleapis.com/token', {
            method: 'POST',
            headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
            body: new URLSearchParams({
                client_id: cfg.clientId ?? '',
                client_secret: cfg.clientSecret ?? '',
                refresh_token: cfg.refreshToken ?? '',
                grant_type: 'refresh_token',
            }),
        });
        const json = (await res.json().catch(() => ({})));
        if (!res.ok || !json.access_token) {
            throw new common_1.BadRequestException(`فشل تجديد رمز الوصول من جوجل — تحقق من Client ID/Secret/Refresh Token (${json.error_description ?? res.status})`);
        }
        return json.access_token;
    }
    async backup(tenantId) {
        const cfg = await this.loadCfg(tenantId);
        const native = !!(cfg.clientId && cfg.clientSecret && cfg.refreshToken);
        const generic = !!(cfg.uploadUrl && cfg.token);
        if (!cfg?.configured || (!native && !generic)) {
            throw new common_1.BadRequestException('خدمة النسخ السحابي (Drive) غير مهيأة — أدخل Client ID و Client Secret و Refresh Token من شاشة الإعدادات (أو uploadUrl و token للوضع البديل)');
        }
        // Build the archive locally first, then upload to the configured destination.
        const local = await this.local.backup(tenantId);
        const fileName = path.basename(local.filePath);
        if (native) {
            const token = await this.refreshAccessToken(cfg);
            const meta = { name: fileName };
            if (cfg.folderId)
                meta.parents = [cfg.folderId];
            const boundary = 'medad-' + crypto.randomUUID();
            const pre = Buffer.from(`--${boundary}\r\nContent-Type: application/json; charset=UTF-8\r\n\r\n${JSON.stringify(meta)}\r\n--${boundary}\r\nContent-Type: application/octet-stream\r\n\r\n`, 'utf8');
            const post = Buffer.from(`\r\n--${boundary}--`, 'utf8');
            const body = Buffer.concat([pre, fs.readFileSync(local.filePath), post]);
            const res = await fetch('https://www.googleapis.com/upload/drive/v3/files?uploadType=multipart&supportsAllDrives=true', {
                method: 'POST',
                headers: { Authorization: `Bearer ${token}`, 'Content-Type': `multipart/related; boundary=${boundary}` },
                body: new Uint8Array(body),
            });
            if (!res.ok) {
                const errText = await res.text().catch(() => '');
                throw new common_1.BadRequestException(`فشل رفع النسخة الاحتياطية إلى Drive (رمز الخطأ: ${res.status}) ${errText.slice(0, 200)}`);
            }
        }
        else {
            const res = await fetch(cfg.uploadUrl, {
                method: 'PUT',
                headers: { Authorization: `Bearer ${cfg.token}`, 'Content-Type': 'application/json' },
                body: fs.readFileSync(local.filePath),
            });
            if (!res.ok)
                throw new common_1.BadRequestException(`فشل رفع النسخة الاحتياطية (رمز الخطأ: ${res.status})`);
        }
        return local;
    }
};
exports.DriveBackupProvider = DriveBackupProvider;
exports.DriveBackupProvider = DriveBackupProvider = __decorate([
    (0, common_1.Injectable)(),
    __metadata("design:paramtypes", [prisma_service_js_1.PrismaService, LocalBackupProvider])
], DriveBackupProvider);
let BackupService = class BackupService {
    prisma;
    localProvider;
    driveProvider;
    constructor(prisma, localProvider, driveProvider) {
        this.prisma = prisma;
        this.localProvider = localProvider;
        this.driveProvider = driveProvider;
    }
    /** Daily auto-backup scheduler (hourly tick; runs when last success > 24h). */
    onModuleInit() {
        const tick = () => {
            void (async () => {
                try {
                    const tenants = await this.prisma.tenant.findMany({ where: { deletedAt: null }, select: { id: true } });
                    for (const t of tenants) {
                        const auto = await (0, ctx_js_1.settingValue)(this.prisma, t.id, 'backup_auto');
                        if (auto?.enabled === false)
                            continue;
                        const last = await this.prisma.backupRun.findFirst({ where: { tenantId: t.id, status: 'success' }, orderBy: { startedAt: 'desc' } });
                        if (last && Date.now() - last.startedAt.getTime() < 24 * 3600 * 1000)
                            continue;
                        await this.run(t.id, 'local', 'system').catch(() => undefined);
                    }
                }
                catch {
                    // scheduler must never crash the API
                }
            })();
        };
        setInterval(tick, 60 * 60 * 1000);
        setTimeout(tick, 15 * 1000);
    }
    async run(tenantId, providerName, actorId) {
        const provider = providerName === 'drive' ? this.driveProvider : this.localProvider;
        const run = await this.prisma.backupRun.create({ data: { tenantId, provider: provider.name, status: 'failed' } });
        try {
            const { filePath, bytes } = await provider.backup(tenantId);
            const done = await this.prisma.backupRun.update({ where: { id: run.id }, data: { status: 'success', filePath, sizeBytes: bytes, finishedAt: new Date() } });
            await this.applyRetention(tenantId);
            await this.prisma.auditLog.create({ data: { tenantId, actorId, action: 'backup', entity: 'backup_runs', entityId: run.id, diff: { provider: provider.name, filePath, bytes } } });
            return done;
        }
        catch (e) {
            const failed = await this.prisma.backupRun.update({ where: { id: run.id }, data: { status: 'failed', message: e.message, finishedAt: new Date() } });
            await this.prisma.auditLog.create({ data: { tenantId, actorId, action: 'backup_failed', entity: 'backup_runs', entityId: run.id, diff: { provider: provider.name, error: e.message } } });
            return failed;
        }
    }
    /** Keep last N local copies (settings backup_keep, default 7). */
    async applyRetention(tenantId) {
        const keep = ((await (0, ctx_js_1.settingValue)(this.prisma, tenantId, 'backup_keep')) ?? { n: 7 }).n;
        const runs = await this.prisma.backupRun.findMany({
            where: { tenantId, provider: 'local', status: 'success' },
            orderBy: { startedAt: 'desc' },
        });
        for (const old of runs.slice(keep)) {
            if (old.filePath && fs.existsSync(old.filePath)) {
                try {
                    fs.unlinkSync(old.filePath);
                }
                catch { /* ignore */ }
            }
        }
    }
    runs(tenantId) {
        return this.prisma.backupRun.findMany({ where: { tenantId }, orderBy: { startedAt: 'desc' }, take: 50 });
    }
    /** Full DR restore from a local backup file (manager only). Restores ALL tables atomically. */
    async restore(tenantId, filePath, actorId) {
        if (!filePath.startsWith(env_js_1.BACKUP_DIR))
            throw new common_1.BadRequestException('مسار النسخة خارج مجلد النسخ');
        if (!fs.existsSync(filePath))
            throw new common_1.BadRequestException('ملف النسخة غير موجود');
        const dump = JSON.parse(fs.readFileSync(filePath, 'utf8'));
        await this.prisma.$transaction(async (db) => {
            for (const model of [...DUMP_ORDER].reverse()) {
                await db[model].deleteMany({});
            }
            for (const model of DUMP_ORDER) {
                const rows = dump[model] ?? [];
                if (rows.length > 0)
                    await db[model].createMany({ data: rows });
            }
            await (0, ctx_js_1.auditTx)(db, { tenantId, actorId, action: 'restore', entity: 'backup_runs', diff: { filePath } });
        }, { timeout: 120000 });
        return { ok: true, file: filePath };
    }
};
exports.BackupService = BackupService;
exports.BackupService = BackupService = __decorate([
    (0, common_1.Injectable)(),
    __metadata("design:paramtypes", [prisma_service_js_1.PrismaService,
        LocalBackupProvider,
        DriveBackupProvider])
], BackupService);
//# sourceMappingURL=backup.service.js.map
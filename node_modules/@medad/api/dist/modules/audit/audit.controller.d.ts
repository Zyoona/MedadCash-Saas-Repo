import type { Response, Request } from 'express';
import { AuditService } from './audit.service.js';
import { type AuthUser } from '../auth/auth.guard.js';
export declare class AuditController {
    private readonly audit;
    constructor(audit: AuditService);
    list(user: AuthUser, actorId?: string, action?: string, entity?: string, from?: string, to?: string, page?: string, pageSize?: string): Promise<{
        total: number;
        page: number;
        pageSize: number;
        rows: ({
            actor: {
                email: string;
                name: string;
                id: string;
            } | null;
        } & {
            tenantId: string;
            branchId: string | null;
            id: string;
            actorId: string | null;
            action: string;
            entity: string;
            entityId: string | null;
            diff: import("@prisma/client/runtime/library.js").JsonValue | null;
            ip: string | null;
            device: string | null;
            createdAt: Date;
        })[];
    }>;
    /** Client-driven read audit: page view / search / print events (§7). */
    event(user: AuthUser, req: Request, body: {
        action: string;
        entity: string;
        entityId?: string;
    }): Promise<{
        ok: boolean;
    }>;
    exportCsv(user: AuthUser, req: Request, res: Response, actorId?: string, action?: string, entity?: string, from?: string, to?: string): Promise<void>;
}
//# sourceMappingURL=audit.controller.d.ts.map
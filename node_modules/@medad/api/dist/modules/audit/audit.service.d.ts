import { PrismaService } from '../../prisma/prisma.service.js';
export interface AuditInput {
    tenantId: string;
    actorId?: string | null;
    branchId?: string | null;
    action: string;
    entity: string;
    entityId?: string | null;
    diff?: unknown;
    ip?: string | null;
    device?: string | null;
}
export declare class AuditService {
    private readonly prisma;
    constructor(prisma: PrismaService);
    /** Standalone log (outside business tx) — used for login and read-audit events. */
    log(p: AuditInput): Promise<void>;
    list(q: {
        tenantId: string;
        actorId?: string;
        action?: string;
        entity?: string;
        from?: string;
        to?: string;
        page?: number;
        pageSize?: number;
    }): Promise<{
        total: number;
        page: number;
        pageSize: number;
        rows: ({
            actor: {
                email: string;
                name: string;
                id: string;
            } | null;
        } & {
            tenantId: string;
            branchId: string | null;
            id: string;
            actorId: string | null;
            action: string;
            entity: string;
            entityId: string | null;
            diff: import("@prisma/client/runtime/library.js").JsonValue | null;
            ip: string | null;
            device: string | null;
            createdAt: Date;
        })[];
    }>;
    /** CSV export (Excel-compatible: UTF-8 BOM + comma separated). */
    exportCsv(q: {
        tenantId: string;
        actorId?: string;
        action?: string;
        entity?: string;
        from?: string;
        to?: string;
    }): Promise<string>;
}
//# sourceMappingURL=audit.service.d.ts.map
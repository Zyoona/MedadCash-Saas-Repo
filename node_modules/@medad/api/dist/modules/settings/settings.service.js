"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.SettingsService = void 0;
const common_1 = require("@nestjs/common");
const prisma_service_js_1 = require("../../prisma/prisma.service.js");
const crypto_util_js_1 = require("../../common/crypto.util.js");
const ctx_js_1 = require("../../common/ctx.js");
const SECRET_KEYS = new Set(['drive_config']);
let SettingsService = class SettingsService {
    prisma;
    constructor(prisma) {
        this.prisma = prisma;
    }
    /** All settings merged: defaults ← tenant ← branch. */
    async getAll(tenantId, branchId) {
        const rows = await this.prisma.setting.findMany({ where: { tenantId } });
        const out = { ...ctx_js_1.SETTING_DEFAULTS };
        for (const r of rows) {
            if (r.branchId === null)
                out[r.key] = this.decode(r.key, r.value);
        }
        if (branchId) {
            for (const r of rows) {
                if (r.branchId === branchId)
                    out[r.key] = this.decode(r.key, r.value);
            }
        }
        return out;
    }
    decode(key, value) {
        if (SECRET_KEYS.has(key) && typeof value === 'string' && value.startsWith('v1:')) {
            try {
                return (0, crypto_util_js_1.decryptJson)(value);
            }
            catch {
                return { configured: false, error: 'failed to decrypt' };
            }
        }
        return value;
    }
    async set(tenantId, key, value, branchId, actorId) {
        if (!(key in ctx_js_1.SETTING_DEFAULTS) && !key.startsWith('custom.')) {
            throw new common_1.BadRequestException(`مفتاح إعداد غير معروف: ${key}`);
        }
        const stored = SECRET_KEYS.has(key) ? (0, crypto_util_js_1.encryptJson)(value) : value;
        await this.prisma.$transaction(async (db) => {
            await db.setting.upsert({
                where: { tenantId_branchId_key: { tenantId, branchId: branchId, key } },
                update: { value: stored },
                create: { tenantId, branchId, key, value: stored },
            }).catch(async () => {
                // fallback when unique-null semantics differ: find+create/update
                const existing = await db.setting.findFirst({ where: { tenantId, branchId, key } });
                if (existing)
                    await db.setting.update({ where: { id: existing.id }, data: { value: stored } });
                else
                    await db.setting.create({ data: { tenantId, branchId, key, value: stored } });
            });
            await (0, ctx_js_1.auditTx)(db, { tenantId, actorId, branchId, action: 'update', entity: 'settings', entityId: key, diff: { key, value: SECRET_KEYS.has(key) ? '[encrypted]' : value } });
        });
        return this.getAll(tenantId, branchId);
    }
};
exports.SettingsService = SettingsService;
exports.SettingsService = SettingsService = __decorate([
    (0, common_1.Injectable)(),
    __metadata("design:paramtypes", [prisma_service_js_1.PrismaService])
], SettingsService);
//# sourceMappingURL=settings.service.js.map
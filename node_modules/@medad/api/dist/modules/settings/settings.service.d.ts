import { PrismaService } from '../../prisma/prisma.service.js';
export declare class SettingsService {
    private readonly prisma;
    constructor(prisma: PrismaService);
    /** All settings merged: defaults ← tenant ← branch. */
    getAll(tenantId: string, branchId?: string | null): Promise<Record<string, unknown>>;
    private decode;
    set(tenantId: string, key: string, value: unknown, branchId: string | null, actorId: string): Promise<Record<string, unknown>>;
}
//# sourceMappingURL=settings.service.d.ts.map
"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.AuthService = void 0;
const common_1 = require("@nestjs/common");
const bcrypt = __importStar(require("bcryptjs"));
const prisma_service_js_1 = require("../../prisma/prisma.service.js");
const jwt_util_js_1 = require("../../common/jwt.util.js");
const env_js_1 = require("../../common/env.js");
const permissions_js_1 = require("../../common/permissions.js");
const audit_service_js_1 = require("../audit/audit.service.js");
let AuthService = class AuthService {
    prisma;
    audit;
    constructor(prisma, audit) {
        this.prisma = prisma;
        this.audit = audit;
    }
    toAuthUser(u) {
        const override = (u.permissionsOverride ?? null);
        return {
            userId: u.id, tenantId: u.tenantId, branchId: u.branchId,
            role: u.role, email: u.email, name: u.name,
            perms: (0, permissions_js_1.expandPerms)(u.role, override),
        };
    }
    async login(email, password, ip, device) {
        const u = await this.prisma.user.findFirst({
            where: { email: email.toLowerCase().trim(), deletedAt: null },
        });
        const fail = async () => {
            if (u) {
                await this.audit.log({ tenantId: u.tenantId, actorId: u.id, action: 'login_failed', entity: 'users', entityId: u.id, ip, device }).catch(() => undefined);
            }
            throw new common_1.UnauthorizedException('بيانات الدخول غير صحيحة');
        };
        if (!u || !u.isActive)
            return fail();
        const ok = await bcrypt.compare(password, u.passwordHash);
        if (!ok)
            return fail();
        const user = this.toAuthUser(u);
        await this.audit.log({ tenantId: u.tenantId, actorId: u.id, action: 'login', entity: 'users', entityId: u.id, ip, device }).catch(() => undefined);
        return { user, ...this.tokens(user) };
    }
    tokens(user) {
        const base = {
            sub: user.userId, tenantId: user.tenantId, branchId: user.branchId,
            role: user.role, email: user.email, name: user.name,
        };
        return {
            accessToken: (0, jwt_util_js_1.signJwt)({ ...base, typ: 'access' }, env_js_1.JWT_ACCESS_SECRET, env_js_1.ACCESS_TTL_SEC),
            refreshToken: (0, jwt_util_js_1.signJwt)({ ...base, typ: 'refresh' }, env_js_1.JWT_REFRESH_SECRET, env_js_1.REFRESH_TTL_SEC),
        };
    }
    async refresh(refreshToken) {
        const payload = (0, jwt_util_js_1.verifyJwt)(refreshToken, env_js_1.JWT_REFRESH_SECRET);
        if (!payload || payload.typ !== 'refresh')
            throw new common_1.UnauthorizedException('انتهت صلاحية الجلسة — يرجى تسجيل الدخول مرة أخرى');
        const u = await this.prisma.user.findFirst({ where: { id: payload.sub, deletedAt: null, isActive: true } });
        if (!u)
            throw new common_1.UnauthorizedException('هذا المستخدم غير نشط — راجع المسؤول');
        const user = this.toAuthUser(u);
        return { user, ...this.tokens(user) };
    }
    /** Per-request: verify access token and load a fresh user (perms may have changed). */
    async authenticate(token) {
        const payload = (0, jwt_util_js_1.verifyJwt)(token, env_js_1.JWT_ACCESS_SECRET);
        if (!payload || payload.typ !== 'access')
            throw new common_1.UnauthorizedException('انتهت صلاحية الجلسة — يرجى تسجيل الدخول مرة أخرى');
        const u = await this.prisma.user.findFirst({ where: { id: payload.sub, deletedAt: null } });
        if (!u)
            throw new common_1.UnauthorizedException('المستخدم غير موجود — يرجى تسجيل الدخول مرة أخرى');
        if (!u.isActive)
            throw new common_1.ForbiddenException('تم تعطيل هذا الحساب — راجع المسؤول');
        return this.toAuthUser(u);
    }
};
exports.AuthService = AuthService;
exports.AuthService = AuthService = __decorate([
    (0, common_1.Injectable)(),
    __metadata("design:paramtypes", [prisma_service_js_1.PrismaService, audit_service_js_1.AuditService])
], AuthService);
//# sourceMappingURL=auth.service.js.map
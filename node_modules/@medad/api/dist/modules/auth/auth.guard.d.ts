import { CanActivate, ExecutionContext } from '@nestjs/common';
import { Reflector } from '@nestjs/core';
import { AuthService, type AuthUser } from './auth.service.js';
export declare const IS_PUBLIC_KEY = "medad:public";
export declare const PERMS_KEY = "medad:perms";
/** Mark a route public (no auth): login/refresh/health. */
export declare const Public: () => import("@nestjs/common").CustomDecorator<string>;
/** Require permission(s) on a route (RBAC §7). */
export declare const RequirePerm: (...perms: string[]) => import("@nestjs/common").CustomDecorator<string>;
/** Inject the authenticated user. */
export declare const CurrentUser: (...dataOrPipes: unknown[]) => ParameterDecorator;
export type { AuthUser };
export declare class AuthGuard implements CanActivate {
    private readonly reflector;
    private readonly auth;
    constructor(reflector: Reflector, auth: AuthService);
    canActivate(context: ExecutionContext): Promise<boolean>;
}
//# sourceMappingURL=auth.guard.d.ts.map
export type Db = any;
export interface ActorCtx {
    userId: string;
    tenantId: string;
    branchId: string | null;
    ip?: string;
    device?: string;
}
/** Resolve the open fiscal year containing `date` (fallback: latest open year). */
export declare function fiscalYearFor(db: Db, tenantId: string, date: Date): Promise<{
    id: string;
    isClosed: boolean;
}>;
export declare function ensureAccount(db: Db, tenantId: string, code: string, name: string, type: string): Promise<void>;
/** Read one setting: branch-level overrides tenant-level; falls back to app defaults. */
export declare const SETTING_DEFAULTS: Record<string, unknown>;
export declare function settingValue<T>(db: Db, tenantId: string, key: string, branchId?: string | null): Promise<T>;
/** Audit row inside the current tx (never fails the business op silently — same tx). */
export declare function auditTx(db: Db, p: {
    tenantId: string;
    actorId?: string | null;
    branchId?: string | null;
    action: string;
    entity: string;
    entityId?: string | null;
    diff?: unknown;
    ip?: string | null;
    device?: string | null;
}): Promise<unknown>;
/** Operation-log row for sync (§6): every business write is logged for batch upload. */
export declare function syncOpTx(db: Db, p: {
    tenantId: string;
    branchId: string;
    deviceId?: string;
    entity: string;
    entityId: string;
    op: 'create' | 'update' | 'delete';
    payload: unknown;
}): Promise<unknown>;
//# sourceMappingURL=ctx.d.ts.map
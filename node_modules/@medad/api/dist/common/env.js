"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.PRODUCT_IMAGE_MAX_BYTES = exports.UPLOAD_DIR = exports.BACKUP_DIR = exports.REFRESH_TTL_SEC = exports.ACCESS_TTL_SEC = exports.JWT_REFRESH_SECRET = exports.JWT_ACCESS_SECRET = void 0;
const path = __importStar(require("path"));
// Runtime configuration. Secrets come from apps/api/.env (loaded in main.ts).
exports.JWT_ACCESS_SECRET = process.env.JWT_ACCESS_SECRET ?? 'medad-dev-access-secret-change-in-prod';
exports.JWT_REFRESH_SECRET = process.env.JWT_REFRESH_SECRET ?? 'medad-dev-refresh-secret-change-in-prod';
exports.ACCESS_TTL_SEC = 60 * 60; // 1 hour (short-lived access)
exports.REFRESH_TTL_SEC = 7 * 24 * 60 * 60; // 7 days
// جذر المشروع مشتق من موقع الملف (src/common في التطوير أو dist/common بعد البناء)
// بدلاً من مسار قرص ثابت، ليعمل النسخ الاحتياطي والرفع من أي مسار.
const PROJECT_ROOT = path.resolve(__dirname, '..', '..', '..', '..');
exports.BACKUP_DIR = process.env.BACKUP_DIR ?? path.join(PROJECT_ROOT, '_tools', 'backups');
exports.UPLOAD_DIR = process.env.UPLOAD_DIR ?? path.join(PROJECT_ROOT, '_tools', 'uploads');
exports.PRODUCT_IMAGE_MAX_BYTES = 5 * 1024 * 1024; // حد حجم صورة الصنف بعد فك الترميز
//# sourceMappingURL=env.js.map
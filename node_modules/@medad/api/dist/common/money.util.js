"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.decToAgora = decToAgora;
exports.agoraToDec = agoraToDec;
exports.mulBps = mulBps;
exports.allocateProRata = allocateProRata;
// Money helpers: integer agora everywhere (no float). DB bridge via Prisma.Decimal strings.
const client_1 = require("@prisma/client");
const shared_types_1 = require("@medad/shared-types");
function decToAgora(d) {
    if (d === null || d === undefined)
        return 0;
    return (0, shared_types_1.toAgora)(String(d));
}
function agoraToDec(a) {
    return new client_1.Prisma.Decimal((0, shared_types_1.fromAgora)(a));
}
/** Multiply by basis points with integer rounding (bps: 0=0%, 1600=16%). */
function mulBps(amountAgora, bps) {
    return Math.round((amountAgora * bps) / 10000);
}
/**
 * Allocate `total` across `weights` proportionally, largest-remainder,
 * so that sum(parts) === total exactly. Integer agora only.
 */
function allocateProRata(total, weights) {
    const sum = weights.reduce((s, w) => s + w, 0);
    if (sum === 0)
        return weights.map(() => 0);
    const exact = weights.map((w) => (w * total) / sum);
    const floors = exact.map((x) => Math.floor(x));
    let remainder = total - floors.reduce((s, x) => s + x, 0);
    const order = exact
        .map((x, i) => ({ i, frac: x - Math.floor(x) }))
        .sort((a, b) => b.frac - a.frac);
    for (const { i } of order) {
        if (remainder <= 0)
            break;
        floors[i] += 1;
        remainder -= 1;
    }
    return floors;
}
//# sourceMappingURL=money.util.js.map
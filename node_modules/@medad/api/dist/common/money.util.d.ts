import { Prisma } from '@prisma/client';
export declare function decToAgora(d: Prisma.Decimal | string | number | null | undefined): number;
export declare function agoraToDec(a: number): Prisma.Decimal;
/** Multiply by basis points with integer rounding (bps: 0=0%, 1600=16%). */
export declare function mulBps(amountAgora: number, bps: number): number;
/**
 * Allocate `total` across `weights` proportionally, largest-remainder,
 * so that sum(parts) === total exactly. Integer agora only.
 */
export declare function allocateProRata(total: number, weights: number[]): number[];
//# sourceMappingURL=money.util.d.ts.map
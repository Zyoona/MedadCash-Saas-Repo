"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
require("reflect-metadata");
const dotenv = __importStar(require("dotenv"));
const fs = __importStar(require("fs"));
const path = __importStar(require("path"));
const express = __importStar(require("express"));
const core_1 = require("@nestjs/core");
const app_module_js_1 = require("./app.module.js");
const env_js_1 = require("./common/env.js");
// Load apps/api/.env explicitly: works both when cwd=apps/api (nest start)
// and when invoked from the repo root; root .env is NOT used.
dotenv.config({ path: path.resolve(process.cwd(), '.env') });
dotenv.config({ path: path.resolve(process.cwd(), 'apps', 'api', '.env') });
async function bootstrap() {
    const app = await core_1.NestFactory.create(app_module_js_1.AppModule, { bodyParser: false });
    app.setGlobalPrefix('api');
    app.enableCors();
    // رفع صور الأصناف يأتي كـ JSON (data URL) — حد كبير لمسار الرفع فقط، والافتراضي 100kb لبقية المسارات
    const imageUploadJson = express.json({ limit: '10mb' });
    const defaultJson = express.json({ limit: '100kb' });
    const isImageUpload = (req) => req.method === 'POST' && /^\/api\/catalog\/products\/[^/]+\/image\/?$/.test(req.originalUrl);
    app.use((req, res, next) => (isImageUpload(req) ? imageUploadJson(req, res, next) : defaultJson(req, res, next)));
    app.use(express.urlencoded({ extended: true, limit: '100kb' }));
    // صور الأصناف المرفوعة — عامة (بدون توكن) لأن <img> لا يرسل Authorization
    try {
        fs.mkdirSync(path.join(env_js_1.UPLOAD_DIR, 'products'), { recursive: true });
        app.useStaticAssets(env_js_1.UPLOAD_DIR, { prefix: '/api/uploads' });
    }
    catch (e) {
        console.warn(`تعذر تهيئة مجلد رفع الصور (${env_js_1.UPLOAD_DIR}) — رفع وعرض الصور معطل`, e);
    }
    const port = Number(process.env.PORT ?? 3000);
    await app.listen(port);
    // eslint-disable-next-line no-console
    console.log(`medad-api listening on :${port}/api`);
}
void bootstrap();
//# sourceMappingURL=main.js.map